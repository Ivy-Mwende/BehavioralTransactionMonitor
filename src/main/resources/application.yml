spring:
  application:
    name: BehavioralTransactionMonitor
  profiles:
    active: dev

# server:
#   servlet:
#     context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics